const { default: isNumber, default: isNumber } = require('./calculate');
const isNumber = require('./calculate');
const calculate = require('./calculate');


describe ("testing calculate task", () => {
    let isNumber = new isNumber;
    let des

})